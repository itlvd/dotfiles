return {
  {
    "nvim-mini/mini.pairs",
    -- config = function(_, opts)
    --   LazyVim.mini.pairs(opts)
    --   -- Remove smart Enter/Backspace so pressing Enter or BS inside a pair
    --   -- behaves like a normal keypress instead of expanding/deleting the pair.
    --   pcall(vim.keymap.del, "i", "<CR>")
    --   pcall(vim.keymap.del, "i", "<BS>")
    -- end,
  },
}
